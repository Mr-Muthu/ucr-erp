# iOS Simulator (shares the host Mac's network — localhost works):
EXPO_PUBLIC_API_URL=http://localhost:4000/api

# Real device (phone must be on the same WiFi as the dev machine):
# EXPO_PUBLIC_API_URL=http://<your-lan-ip>:4000/api
# find your LAN IP with: ipconfig getifaddr en0
